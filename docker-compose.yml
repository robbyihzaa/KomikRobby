services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_DB: comics
      POSTGRES_USER: comics
      POSTGRES_PASSWORD: comics
    ports:
      - "5432:5432"
    volumes:
      - comic_pg:/var/lib/postgresql/data
volumes:
  comic_pg:
